import { Authentication } from "../../components/authentication/authentication.js";

function Login(){
    return(
        <div className = "login">
            <Authentication/>
        </div>
    )
}

export default Login;